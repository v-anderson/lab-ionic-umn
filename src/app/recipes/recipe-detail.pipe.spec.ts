import { RecipeDetailPipe } from './recipe-detail.pipe';

describe('RecipeDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
